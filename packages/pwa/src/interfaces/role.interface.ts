export enum Role {
  USER = 45,
  STUDENT = 50,
  TEACHER = 100,
  CONCIERGE = 200,
  FACILITY_MANAGER = 500,
  ADMIN = 800,
  SUPERADMIN = 900,
}
// ALWAYS use the same values as in the API for consistency -> users.entity.ts
